//: [Previous](@previous)

import Foundation

// grab the last digit using mod 10
// add the strings representing last digit together
// divide the num by 10 and store new number to pull appart
// continue till both numbers = 0

// put both into

func addStrings(strA: String, strB: String) {
    var tempA = Int(strA)
    var tempB = Int(strB)
    
    
}
