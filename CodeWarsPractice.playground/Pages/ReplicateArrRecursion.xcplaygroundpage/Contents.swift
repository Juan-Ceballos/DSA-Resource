//: [Previous](@previous)

import Foundation

/*
function replicate(times, number, arr) {
    if (times < 1) {
    return []
  }
  
  return [number].concat(replicate(times - 1, number, arr))
  // could take out arr paramater, and use the return of an array being added what is in
  // the return of the next array
}
*/
