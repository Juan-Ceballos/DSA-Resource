//: [Previous](@previous)

import Foundation

/*
 function countPositivesSumNegatives(input) {
   if (input === null || input === []) {
     return []
   }
   
   let pos = 0
   let neg = 0
   
   for (let i = 0; i < input.length; i++) {
     if (input[i] > 0) {
       pos += 1
     } else if (input[i] < 0) {
       neg += input[i]
     }
   }
   
   if (neg === 0 && pos === 0 && input.length >= 0) {
     console.log("hey")
     return []
   }
   
   return [pos, neg]
   
 }

 console.log(countPositivesSumNegatives([1, 2, -3]))
 */
