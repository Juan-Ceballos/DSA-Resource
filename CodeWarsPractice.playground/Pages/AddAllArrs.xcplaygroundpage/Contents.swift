//: [Previous](@previous)

import Foundation

//function arraySum(arr) {
//  let temp = []
//  for (let i = 0; i < arr.length; i++) {
//    if (typeof arr[i] === 'number') {
//      temp.push(arr[i])
//    } else {
//      if (typeof arr[i] === 'string' || typeof arr[i] === 'function') {
//       temp.push(0)
//      }
//      else {
//        temp = arr[i].reduce((a, b) => a.concat(b), temp)
//      }
//    }
//  }
//  temp = temp.filter((a) => typeof a === 'number')
//  temp = temp.reduce((a, b) => a + b)
//  return temp
//
//}
