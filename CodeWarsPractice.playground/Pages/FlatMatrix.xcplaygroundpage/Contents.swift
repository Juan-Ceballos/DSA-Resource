//: [Previous](@previous)

import Foundation

/*
 function gridIndex(grid, indices) {
   let result = ''
   let flatGrid = grid.flat()

   for (let i = 0; i < indices.length; i++) {
     let num = indices[i]
     result += flatGrid[num - 1]
   }
   return result
 } 
 */
