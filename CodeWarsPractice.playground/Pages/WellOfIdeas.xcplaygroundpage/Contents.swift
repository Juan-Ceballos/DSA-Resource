//: [Previous](@previous)

import Foundation
/*
function well(x){
  let tempArr = [].concat.apply([], x)
  let goodCount = 0
  for (let i = 0; i < tempArr.length; i++) {
    let review = tempArr[i]
    if (typeof review == "string") {
      if (review.toLowerCase() === "good") {
      goodCount += 1
      }
    }
  }
  
  if (goodCount > 2) {
    return "I smell a series!"
  } else if (goodCount > 0) {
    return "Publish!"
  } else {
    return "Fail!"
  }
  
}
*/
