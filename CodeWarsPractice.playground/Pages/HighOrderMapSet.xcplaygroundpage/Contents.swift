//: [Previous](@previous)

import Foundation

/*
 function isSameLanguage(list) {
   let languageArr = list.map(student => student.language)
   let languageSet = new Set(languageArr)
   return languageSet.size == 1
 }
 
 
 function isSameLanguage(list) {
   return list.every(e => e.language === list[0].language);
 }
 */
